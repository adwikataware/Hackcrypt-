// QUICK NAVIGATION GUIDE
// ======================

📱 PAGES (Main views)
├── pages/Home.jsx         → Upload interface + info cards
└── pages/Results.jsx      → Analysis results dashboard

🧩 COMPONENTS

  Upload & Processing
  ├── UploadZone.jsx       → Drag-drop + URL input
  └── ProcessingIndicator.jsx → Loading bar + progress

  Results Display
  ├── SummaryCard.jsx      → Main deepfake verdict + scores
  ├── ConfidenceBreakdown.jsx → 5 score breakdown visualization
  ├── ThreatBadge.jsx      → HIGH/MEDIUM/LOW status
  └── Tabs.jsx             → Tab navigation component

  Result Tabs (Detailed Analysis)
  └── tabs/
      ├── OverviewTab.jsx       → Primary findings + evidence
      ├── VisualAnalysisTab.jsx → Heatmaps + artifacts
      ├── AudioAnalysisTab.jsx  → Spectrograms + anomalies
      └── TimelineTab.jsx       → Manipulation timeline

🎨 STYLING

  Colors (in tailwind.config.js)
  ├── bg-dark               (#0a0a0a)
  ├── bg-dark-secondary     (#1a1a1a)
  ├── bg-dark-tertiary      (#2a2a2a)
  ├── bg-gray-accent        (#404040)
  ├── text-text-primary     (#ffffff)
  └── text-text-secondary   (#b0b0b0)

  Utility Classes (in src/index.css)
  ├── .card                 → Card component style
  ├── .btn-primary          → White button
  ├── .btn-secondary        → Border button
  └── .transition-smooth    → 300ms transitions

⚙️ CONFIG FILES

  ├── vite.config.js        → Vite build + proxy setup
  ├── tailwind.config.js    → Colors + fonts
  ├── postcss.config.js     → CSS processing
  ├── package.json          → Dependencies
  └── .env.example          → Environment variables

📡 API ENDPOINTS

  POST /api/detect
  Request:  { video: File } or { video_url: string }
  Response: {
    overall_confidence: number,
    classification: string,
    threat_level: "LOW|MEDIUM|HIGH",
    visual_score: number,
    audio_score: number,
    temporal_score: number,
    lipsync_score: number,
    metadata_score: number
  }

🚀 QUICK COMMANDS

  npm install              → Install dependencies
  npm run dev              → Start dev server (http://localhost:5173)
  npm run build            → Build for production
  npm run preview          → Preview production build

💡 DESIGN PHILOSOPHY

  ✓ Black & grey color scheme
  ✓ Clean, minimal, Figma-style UI
  ✓ Inter font for modern look
  ✓ Smooth transitions (300ms)
  ✓ Clear visual hierarchy
  ✓ Responsive (mobile-first)
  ✓ Professional but accessible

📝 NEXT STEPS

  Phase 1: Setup ✓ DONE
  Phase 2: Install npm + run dev
  Phase 3: Backend integration (connect API)
  Phase 4: Add Tier 2 features (heatmaps, timeline, threats)
  Phase 5: Tier 3 polish (animations, toasts, accessibility)
